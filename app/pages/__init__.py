"""Streamlit pages for Spotify Music Intelligence.

Pages define a ``render()`` function and only invoke it when a Streamlit
runtime is active, so importing any page from pytest does not touch the
widget API.
"""

from __future__ import annotations
